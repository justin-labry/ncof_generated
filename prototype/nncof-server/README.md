# OpenAPI generated FastAPI server

This Python package is automatically generated by the [OpenAPI Generator](https://openapi-generator.tech) project:

- API version: 0.1.0
- Generator version: 7.17.0-SNAPSHOT
- Build package: org.openapitools.codegen.languages.PythonFastAPIServerCodegen

## Requirements.

Python >= 3.7

## Installation & Usage

To run the server, please execute the following from the root directory:

```bash
pip3 install -r requirements.txt
PYTHONPATH=src uvicorn nncof.main:app --host 0.0.0.0 --port 8080
```

and open your browser at `http://localhost:8080/docs/` to see the docs.

## .env

nncof-server/ 디렉토리에 .env 파일을 생성하고 아래 내용을 서버설정에 맞게 삽입한다.


```sh
NCOF_NOTIFICATION_BASE_URI = "http://localhost:8000/notifications"

```


## Running with Docker

To run the server on a Docker container, please execute the following from the root directory:

```bash
docker compose up --build
```

## Tests

To run the tests:

```bash
pip3 install pytest
PYTHONPATH=src pytest tests
```


## 새로운 절차

pyproject.toml 삭제
uv init .
uv add fastapi
uv add uvicorn

create run.sh and fill it like below:
```sh
export PYTHONPATH=src
uv run uvicorn nncof.main:app --host 0.0.0.0 --port 8080
```

sh run.sh 후 실행

아래 오류 발생 시 
```sh
  File "D:\projects\2026\ncof_yaml\converted_yaml\nncof-server\src\nncof\apis\individual_ncof_event_subscription_transfer_document_api_base.py", line 11, in <module>
    from nncof.security_api import get_token_oAuth2ClientCredentials
  File "D:\projects\2026\ncof_yaml\converted_yaml\nncof-server\src\nncof\security_api.py", line 24, in <module>
    security_scopes: SecurityScopes, token: str = Depends(oauth2_)
                                                          ^^^^^^^
NameError: name 'oauth2_' is not defined. Did you mean: 'OAuth2'?
```
