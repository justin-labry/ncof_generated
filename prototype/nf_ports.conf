# ==============================================================================
# NCOF Mock-up NF 포트 단일 출처 (Single Source of Truth)
# ------------------------------------------------------------------------------
# 포트를 바꾸려면 이 파일만 수정하고 서버를 재시작하세요.
#   - 각 NF의 run.sh 가 이 파일을 읽어 자신의 listen 포트로 사용
#   - NCOF(nrf.py)가 이 파일을 읽어 다른 NF를 찾는 주소로 사용
#   - GUI(vite.config.ts)가 NCOF_PORT 를 읽어 dev 프록시 타깃으로 사용
# 이 파일은 git 으로 추적됩니다(.env 과 달리 clone 시 함께 공유됨).
# ==============================================================================
NF_HOST=127.0.0.1
NCOF_PORT=9000
SMF_PORT=9001
NEF_PORT=9002
UPF_PORT=9003
PCF_PORT=9004
